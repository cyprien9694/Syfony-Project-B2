{% extends 'base.html.twig' %}

{% block title %}Liste des étoiles{% endblock %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
{% endblock %}

{% block body %}
<h1>Liste des étoiles 🌟</h1>

<a href="{{ path('star_new') }}" class="btn btn-primary">Ajouter une étoile</a>

<table class="table mt-3">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Description</th>
            <th>Image</th>
            <th>Créé le</th>
        </tr>
    </thead>
    <tbody>
        {% for star in stars %}
            <tr>
                <td>{{ star.id }}</td>
                <td>{{ star.name }}</td>
                <td>{{ star.description }}</td>
                <td>
                    {% if star.image %}
                        <img src="{{ star.image }}" alt="{{ star.name }}" width="50">
                    {% endif %}
                </td>
                <td>{{ star.createdAt|date('d/m/Y H:i') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucune étoile trouvée.</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
