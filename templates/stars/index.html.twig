{% extends 'base.html.twig' %}

{% block title %}Liste des étoiles{% endblock %}

{% block body %}
<h1>Liste des étoiles 🌟</h1>

<a href="{{ path('star_new') }}" class="btn btn-primary mb-3">Ajouter une étoile</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Description</th>
            <th>Image</th>
            <th>Créé le</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        {% for star in stars %}
            <tr>
                <td>{{ star.id }}</td>
                <td>{{ star.name }}</td>
                <td>{{ star.description }}</td>
                <td>
                    {% if star.image %}
                        <img src="{{ star.image }}" alt="{{ star.name }}" width="50">
                    {% endif %}
                </td>
                <td>{{ star.createdAt|date('d/m/Y H:i') }}</td>
                <td>
                    <a href="{{ path('star_edit', {'id': star.id}) }}" class="btn btn-warning btn-sm">Modifier</a>
                    <a href="{{ path('star_delete', {'id': star.id}) }}"
                       onclick="return confirm('Voulez-vous vraiment supprimer cette étoile ?');"
                       class="btn btn-danger btn-sm">Supprimer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucune étoile trouvée.</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
