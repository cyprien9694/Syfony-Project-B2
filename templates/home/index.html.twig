{% extends 'base.html.twig' %}

{% block title %} Accueil – Sky of Tonight{% endblock %}

{% block stylesheets %}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
<link rel="stylesheet" href="{{ asset('css/style.css') }}">
{% endblock %}

{% block javascripts %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="{{ asset('js/audio.js') }}"></script>
{% endblock %}

{% block icon %}
<link rel="icon" href="{{ asset('image/star.png') }}" type="image/png">
<link rel="shortcut icon" href="{{ asset('image/star.png') }}">
{% endblock %}


{% block body %}
<audio id="bg-music" loop>
    <source src="{{ asset('audio/1.Abandoned_Outpost.mp3') }}" type="audio/mpeg">
</audio>
<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">

        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
            🌌 Sky of Tonight
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto">

                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_home') }}">🏠 Accueil</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_star') }}">⭐ Étoiles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_star_day') }}">🌟 Étoile du jour</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contact') }}">ℹ️ Contact</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_comment') }}">💬 Commentaires</a>
                </li>
                <ul class="navbar-nav">
                    {% if app.user %}
                    <li class="nav-item">
                        <div class="d-flex align-items-center">
                            <span class="text-primary me-2">
                                <i class="fas fa-user-circle"></i> {{ app.user.email|slice(0, 20) }}{{
                                app.user.email|length > 20 ? '...' : '' }}
                            </span>
                            <a class="nav-link" href="{{ path('app_logout') }}">🚪 Se déconnecter</a>
                        </div>
                    </li>
                    {% endif %}
                </ul>
            </ul>
        </div>
    </div>
</nav>

<!-- SECTION HERO 🏠 Accueil -->
<section id="home-hero">

    <!-- Fond étoilé -->
    <div class="stars"></div>

    <div class="container text-center">
        <h1>Bienvenue sur <span>Sky of Tonight</span></h1>

        <p class="lead">
            Explorez les étoiles, constellations et galaxies visibles ce soir.
            Partagez vos observations et voyagez dans l’univers.
        </p>

        <div class="hero-actions">
            <a href="{{ path('app_star') }}" class="btn btn-primary btn-lg me-3">
                ⭐ Explorer les étoiles
            </a>
            <a href="{{ path('app_comment') }}" class="btn btn-outline-light btn-lg">
                💬 Voir les commentaires
            </a>
        </div>
    </div>
</section>
<section id="sound-action">
    <button id="toggle-sound" class="sond-btn">🔇</button>
</section>
{% endblock %}