{# templates/traitement.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Traitement du formulaire{% endblock %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/stylesss.css') }}">
{% endblock %}

{% block body %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
            📩 Traitement du contact
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_about') }}">ℹ️ Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<section id="contact-form">
    <div class="stars"></div>

    <div class="container text-center">

        <h1>Merci pour votre message 🌟</h1>

        <div class="alert alert-success mt-4">
            <p><strong>Nom :</strong> {{ name }}</p>
            <p><strong>Email :</strong> {{ email }}</p>
            <p><strong>Sujet :</strong> {{ subject }}</p>
            <p><strong>Message :</strong></p>
            <p>{{ message }}</p>
        </div>

        <a href="{{ path('app_about') }}" class="btn btn-outline-light mt-3">
            ⬅️ Retour au formulaire
        </a>

    </div>
</section>

{% endblock %}
